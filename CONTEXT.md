# Shale Reader Context

本上下文统一阅读页的数据浏览与选择词汇，使状态规则集中在 Reader 工作区 Module 中，而不是散落在页面调用者里。

## Language

**Reader 工作区**：
承载订阅源浏览、文章查询和当前文章选择状态的阅读会话。
_Avoid_: Reader 页面、主页面状态

**阅读上下文**：
决定当前文章集合的 Feed、标签、列表筛选和搜索条件组合。
_Avoid_: 查询参数、过滤状态

**文章选择**：
Reader 工作区中至多一个当前展开文章的身份及其列表快照。
_Avoid_: 当前卡片、详情状态

## Relationships

- 一个 **Reader 工作区** 同时拥有一个 **阅读上下文** 和至多一个 **文章选择**
- **阅读上下文** 变化会清除现有 **文章选择** 并启动新的文章查询
- **文章选择** 来自当前 **阅读上下文** 返回的文章集合

## Example dialogue

> **Dev:** “用户从 Feed 切到标签时，需要保留当前文章吗？”
> **Domain expert:** “不需要；这是新的 **阅读上下文**，所以 **Reader 工作区** 应清除旧的 **文章选择**。”

## Flagged ambiguities

- “Reader”既可指整个阅读功能，也可指文章详情视图；本上下文使用 **Reader 工作区** 表示前者，文章详情视图不使用该名称。
